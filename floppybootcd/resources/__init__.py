"""Static asset package — icons and other non-code resources.

Files:
    icon.svg   — canonical pixel-art floppy disk source (64x64 viewBox)
    icon.png   — 512x512 raster, used at runtime for QIcon
    icon.ico   — Windows multi-resolution icon, used by PyInstaller --icon
    icon.icns  — macOS icon set, used by PyInstaller --icon
"""
